<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :shipname %>
    <%= form.text_field :shipname, id: :order_shipname %>
  </div>

  <div class="field">
    <%= form.label :shipaddress %>
    <%= form.text_area :shipaddress, id: :order_shipaddress %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :order_phone %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :order_city %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
