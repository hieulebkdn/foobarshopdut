<%= form_with(model: brand, local: true) do |form| %>
<% if brand.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

  <ul>
    <% brand.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :name %>
  <%= form.text_field :name, id: :brand_name %>
</div>

<div class="field">
  <%= form.label :description %>
  <%= form.text_area :description, id: :brand_description %>
</div>

<div class="field">
  <%= form.label :image %>
  <%= form.file_field :image, id: :brand_image %>
  <%= image_tag @brand.image.url %>
  <%= image_tag @brand.image (:medium) %>
  <%= image_tag @brand.image (:thumb) %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>
