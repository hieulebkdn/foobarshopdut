<% provide(:title, "SAMSUNG") %>
<%= render 'layouts/header1' %>
<div class="product-big-title-area">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="product-bit-title text-center">
                        <h2>SAMSUNG</h2>
                    </div>
                </div>
            </div>
        </div>
    </div>
<% @products.each do |product| %>
    <% if product.brand_id == 3 %>
      <%= render 'index_form', product: product %>
    <%end%>
  <% end %>