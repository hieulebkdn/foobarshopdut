$("product-gallery").html("<%= escape_javascript(render 'reviews/_mylayout') %> #This basically says, find the div on the current page with a matching id and then append the partial named _order_table.html.erb to it.
